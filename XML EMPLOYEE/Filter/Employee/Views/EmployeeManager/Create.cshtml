@model Employee.Models.EmployeeManager

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>EmployeeManager</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.strManager)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.strManager)
            @Html.ValidationMessageFor(model => model.strManager)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
