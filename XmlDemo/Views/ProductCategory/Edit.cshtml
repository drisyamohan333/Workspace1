@model XmlDemo.Models.ProductCategory

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ProductCategory</legend>

        @Html.HiddenFor(model => model.ProductCategoryID)

        <div class="editor-label">
            @Html.LabelFor(model => model.strCategory)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.strCategory)
            @Html.ValidationMessageFor(model => model.strCategory)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
